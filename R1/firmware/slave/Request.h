
typedef enum
{
  POLL,
  ASSERT_RESB,
  RELEASE_RESB,
  SELECT_SLAVE_0 =    0x10,
  SELECT_SLAVE_1,
  SELECT_SLAVE_2,
  SELECT_SLAVE_3,
  SELECT_SLAVE_4,
  SELECT_SLAVE_5,
  SELECT_SLAVE_6,
  RELEASE_SLAVE =     0x17,

  QUERY_UART_STATUS = 0x40,
  CONFIGURE_UART,
  DEQUEUE_UART_DATA,
  SEND_VIA_UART,
}
Request;

